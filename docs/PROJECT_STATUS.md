# SynQ Project Status

**Review date:** 13 August 2026
**Review lineage:** Recovery work began from `b4ae020`; the original recovery baseline is tagged `v1.0.0-review`, and later verification updates are recorded in this status file and the evidence log.

> This file is the current operational-status record. It deliberately distinguishes **source present** from **capability verified**.

## Verified Review Results

| Area | Review outcome | Evidence and boundary |
|---|---|---|
| Core compiler library | **Builds in the recovery profile** | On Ubuntu 24.04 with CMake 3.28, GCC 13, `nlohmann-json3-dev`, and `libssl-dev`, `cmake -S compiler -B <build>` followed by `cmake --build <build>` produced `libsynq_lib.a`. The recovery profile excludes incomplete optional targets. `pybind11` is required only for the optional binding target. |
| Compiler CLI | **Not verified** | The checked-in command-line entry point is disabled by default because it has unresolved API and syntax errors. No `synq` command-line executable is claimed. |
| Compiler smoke suite | **Passes locally and in [Compiler Core #29](https://github.com/TangoSplicer/SynQ/actions/runs/31837377648)** | `ctest --test-dir <build> --output-on-failure` completed with 17/17 focused smoke and interoperability tests passing. They cover binary-format round-trip and malformed-input bounds handling; a bounded line-oriented recovery parser with declarations, comments, explicit `q[index]` operands, alpha-gated literal-angle parameter syntax, structured parser diagnostics, typed measurements, bounded semantic checks, and Alpha-gated Boolean-literal-or-resolved-identifier classical control nodes; runtime placeholder paths; feature registry state/opt-in behavior; compiled C and Rust opaque-ABI consumers; test-only Common Lisp CFFI, Clojure/JNA, and Mercury C-backend consumers through the same opaque C ABI; strict OpenQASM 3 export; reference-parser acceptance; and Qiskit downstream conversion of the generated fixture. The parser grammar is intentionally narrow and is not a complete SynQ language implementation. |
| OpenQASM 3 source export | **Passes locally for a bounded subset with two external validation paths** | `synq_openqasm3_exporter_smoke` verifies exact OpenQASM 3 output for `h`, `x`, `y`, `z`, `cx`, `bell_pair`, and literal-angle `rx`, `ry`, `rz`, and `p` gates, including explicit qubit operands, allocation, and diagnostic rejection of unsupported statements or malformed operands. `synq_openqasm3_reference_parse` validates parser acceptance with pinned `openqasm3[parser]==1.0.1`; `synq_openqasm3_qiskit_import` imports the same output with `qiskit-qasm3-import==0.6.0` and checks its expected circuit shape. This does not establish semantic equivalence, execute, or submit to a provider. |
| Experimental language controls | **Locally tested alpha-gate foundation** | `FeatureRegistry` records named Alpha, Beta, and Stable gates. The recovery parser recognizes only the exact file-scoped form `#[experimental(feature = "parameterized-quantum-gates")]`; it rejects unknown annotations and rejects the parameterized quantum syntax when that alpha gate is absent. The current registry and parser tests do not constitute a general feature, permission, or package-management system. |
| Structured parser diagnostics | **Remotely validated recovery-profile foundation** | `ParseResult` owns a successful `ProgramNode` and carries typed diagnostics with code, severity, one-based half-open source span, message, and help. The recovery parser reports `SYNQ-P001` through `SYNQ-P007` for the documented failure categories; `parseFile()` remains a raw-pointer compatibility wrapper that renders diagnostics to standard error. The new smoke test covers every initial parser code and formatting, and is included in [Compiler Core #9](https://github.com/TangoSplicer/SynQ/actions/runs/31719597241). This is not yet lexer recovery, multi-error reporting, a type checker, an LSP interface, or a general diagnostic protocol. |
| Typed quantum AST | **Remotely validated recovery-profile foundation** | The parser now turns accepted `quantum` statements into `QuantumGateNode` values with a bounded `QuantumGateKind`, original source name, optional literal angle, ordered numeric qubit indices, and line. The OpenQASM 3 exporter lowers those nodes directly and retains a temporary adapter only for direct legacy `InstructionNode` fixtures. This behavior is included in [Compiler Core #10](https://github.com/TangoSplicer/SynQ/actions/runs/31720290203). It is not a complete type system, qubit ownership model, measurement model, or Hybrid IR. |
| Typed classical literals | **Remotely validated recovery-profile foundation** | Parsed `let` declarations retain their exact source text and receive `Integer`, `Decimal`, `Boolean`, `QuotedString`, or explicit `SourceText` classification. Direct legacy `DeclarationNode` construction defaults to `SourceText`, preserving its constructor and rendering behavior. This behavior is included in [Compiler Core #11](https://github.com/TangoSplicer/SynQ/actions/runs/31720781396). It is not expression parsing, type checking, numeric evaluation, classical runtime semantics, or a Hybrid IR. |
| Typed gate-shape validation | **Remotely validated recovery-profile semantic boundary** | After `QuantumGateNode` construction, known fixed and parameterized gates receive compiler-neutral shape validation. `SYNQ-S001` through `SYNQ-S003` reject invalid known angles/arity through the existing structured diagnostic and C ABI paths. `Unknown` gates remain parseable for future language work but are still rejected by the bounded OpenQASM exporter. This behavior is included in [Compiler Core #12](https://github.com/TangoSplicer/SynQ/actions/runs/31721517239). It is not general quantum semantic validation, circuit equivalence checking, provider validation, or execution. |
| AST source provenance | **Remotely validated recovery-profile foundation** | Parsed `DeclarationNode`, `InstructionNode`, and `QuantumGateNode` values now own the parser’s existing one-based statement `SourceSpan`, with line-field agreement verified by parser smoke coverage. Direct legacy constructors retain their signatures and carry `{line, 0, 0}` unknown-column spans. This behavior is included in [Compiler Core #13](https://github.com/TangoSplicer/SynQ/actions/runs/31722040872). It is not a lexer, byte-offset source map, multi-line span system, IDE protocol, public AST ABI, or Hybrid IR. |
| Minimal typed Hybrid IR | **Remotely validated internal recovery-profile foundation** | `HybridProgram` preserves ordered typed declarations, quantum gates, and measurements as an internal C++ variant sequence, copying literal hints, gate fields, and source spans without retaining AST pointers. It rejects parser-retained legacy instructions with provenance-carrying `SYNQ-H001` rather than inventing semantics. [Compiler Core #23](https://github.com/TangoSplicer/SynQ/actions/runs/31803349469) and the manual repeat [#24](https://github.com/TangoSplicer/SynQ/actions/runs/31803368082) both reported 14/14 CTest checks for revision `772ede7`. This is not a public ABI, serialized format, type checker, name resolver, qubit/resource model, result-value model, control-flow representation, backend lowering path, or execution engine. |
| Bounded scoped name resolution | **Remotely validated internal recovery-profile foundation** | `resolve_hybrid_names` resolves whole-identifier declaration initializers and Alpha control conditions only to earlier top-level declarations. It propagates existing literal static types, preserves all other source text as opaque, and rejects unknown or forward initializer/condition references with provenance-carrying `SYNQ-R001`/`SYNQ-R002`. A condition resolved to a non-Boolean type returns `SYNQ-T001` instead of a partial resolved program. [Compiler Core #29](https://github.com/TangoSplicer/SynQ/actions/runs/31837377648) reported 17/17 CTest checks for revision `33e98c2`. This is not nested lexical scoping, module/import resolution, expression resolution, general type checking, assignment, mutability, qubit allocation, result handling, a public ABI, or runtime execution. |
| Bounded typed classical control flow and expressions | **Remotely validated Alpha recovery-profile foundation** | After `#[experimental(feature = "classical-control-flow")]`, the parser accepts Boolean-literal or whole-identifier `if ... then ...` and `while ... do ...` forms with one typed quantum-gate or measurement body. Declarations carry bounded literal, identifier-reference, or opaque-source internal expression classifications; only an earlier resolved Boolean declaration may satisfy an identifier condition. Unknown/forward references use `SYNQ-R002`; non-Boolean references use `SYNQ-T001`; malformed forms/bodies retain `SYNQ-P009`/`SYNQ-P010`. [Compiler Core #29](https://github.com/TangoSplicer/SynQ/actions/runs/31837377648) reported 17/17 CTest checks for revision `33e98c2`. This is not general expressions, runtime values, variable conditions beyond whole resolved identifiers, assignment, `else`, blocks, nesting, loop/branch execution, result handling, control-flow lowering, simulation, provider integration, or hardware submission. |
| Bounded declaration validation | **Remotely validated recovery-profile classical semantic boundary** | Within one parse invocation, the parser rejects a repeated top-level `let` identifier with `SYNQ-S004` at the later declaration and a reference to its original line. Names remain case-sensitive and separate parse invocations remain independent. This behavior is included in [Compiler Core #14](https://github.com/TangoSplicer/SynQ/actions/runs/31722554030). It is not a scoped symbol table, module resolver, variable evaluator, assignment system, expression engine, or type checker. |
| Typed measurement source export | **Remotely validated recovery-profile quantum boundary** | `measure q[index]` builds a provenance-carrying `MeasurementNode`; it rejects malformed operand forms with `SYNQ-P008`. The OpenQASM exporter emits a matching classical register only when measurement exists and lowers to `c[index] = measure q[index];`. The generated fixture is accepted by the reference OpenQASM parser and Qiskit importer, and this increment is included in [Compiler Core #15](https://github.com/TangoSplicer/SynQ/actions/runs/31723306294). This is not simulation, returned results, classical control flow, an execution model, a provider integration, or hardware submission. |
| Rust C-ABI consumer proof | **Remotely validated interoperability foundation** | A dependency-free Rust 1.75 consumer declares only the opaque C ABI, verifies ABI v1 identity, parses/exports a measurement fixture, releases every library allocation, and asserts a missing-file diagnostic. The CMake recovery profile compiles it with `rustc`, registers it as CTest, and it is included in [Compiler Core #16](https://github.com/TangoSplicer/SynQ/actions/runs/31724123316). This is not a crate, Rust-native ABI, safe wrapper, generated binding, Rust backend, or claim of full Rust interoperability. |
| In-memory C-ABI parsing | **Remotely validated interoperability foundation** | `synq_parse_source` parses one NUL-terminated source buffer using the same bounded parser stream path as file parsing and does not retain caller storage. Compiled C and Rust consumers parse/export a measurement fixture from memory, free the returned opaque handle/output, and assert a `<memory>` `SYNQ-P008` diagnostic. This behavior is included in [Compiler Core #17](https://github.com/TangoSplicer/SynQ/actions/runs/31724839027). It is not a length-aware byte API, embedded-NUL transport, encoding validator, streaming parser, REPL, editor interface, or general embedding API. |
| Common Lisp CFFI consumer proof | **Remotely validated interoperability foundation** | A test-only `libsynq_ffi.so` is linked inside the CMake build directory from the existing opaque C ABI. SBCL 2.2.9 and Debian `cl-cffi` load it, validate ABI identity, parse/export a measurement source in memory, free every C allocation, and assert `<memory>` `SYNQ-P008`; this behavior is included in [Compiler Core #18](https://github.com/TangoSplicer/SynQ/actions/runs/31725431911). This is not an installed shared library, CFFI/ASDF package, Quicklisp project, portable Lisp binding, safe Lisp API, or claim of full Common Lisp interoperability. |
| Clojure/JNA consumer proof | **Remotely validated interoperability foundation** | A CMake CTest script uses Debian Clojure 1.11.1 and JNA 5.14.0 to load test-only build-directory `libsynq_ffi.so` by absolute path. It validates ABI v1 identity, parses and exports an in-memory measurement source, frees every returned allocation, and asserts `<memory>` `SYNQ-P008`. The 12/12 remote recovery-profile result is included in [Compiler Core #19](https://github.com/TangoSplicer/SynQ/actions/runs/31726350746) for commit `cccf61b`. This is not a Clojure library, Java API, JNI bridge, installed shared library, portable JVM binding, safe wrapper, or claim of full Clojure interoperability. |
| Mercury C-backend consumer proof | **Remotely validated interoperability foundation** | Mercury 22.01.8 is built in an isolated cache from an official source distribution with a checked SHA-512 digest, then compiles a C-backend `pragma foreign_proc` CTest consumer linked to build-directory `libsynq_ffi.so`. It validates ABI identity, in-memory measurement parse/export ownership, and `<memory>` `SYNQ-P008`; [Compiler Core #22](https://github.com/TangoSplicer/SynQ/actions/runs/31729572407) generated the consumer and reported 13/13 passing CTest checks for commit `ef0505f`. This is not a Mercury package, foreign type, installed compiler, multi-backend support, portable binding, or claim of full Mercury interoperability. |
| Native C ABI | **Remotely validated experimental v1 foundation** | `compiler/include/synq/synq_ffi.h` exposes opaque program handles, version reporting, file and in-memory parsing, bounded OpenQASM 3 export, status codes, diagnostics, and library-owned cleanup functions. Its C, Rust, test-only Common Lisp, test-only Clojure/JNA, and test-only Mercury C-backend consumers use the same opaque v1 contract in [Compiler Core #22](https://github.com/TangoSplicer/SynQ/actions/runs/31729572407). This is not yet a published stable ABI, a shared-library distribution, a Rust crate/safe wrapper, or a Mercury/Lisp/Clojure binding. |
| Legacy compiler tests | **Not verified** | Legacy test sources fail when enabled and are disabled by default. No suite-wide pass count, coverage percentage, or performance result is claimed by this record. |
| Python bindings | **Not verified** | The binding target is disabled by default. A JSON-conversion repair was made, but the optional target still needs dedicated build and test coverage. |
| Frontend dependency configuration | **Installs locally with a frozen lockfile** | `pnpm install --frozen-lockfile --prefer-offline` completes after moving the package override to `pnpm-workspace.yaml`, regenerating the lockfile, and removing an unused Vite plugin with an incompatible peer range. |
| Frontend application type-check and build | **Pass locally; optional catalog contract browser-checked** | `pnpm run check` and `pnpm run build` both complete. Route and interactive-example boundaries are lazy-loaded, and explicit vendor chunking reduced the former single 689 kB JavaScript bundle to a 25 kB home chunk plus separate route/vendor chunks; Vite no longer emits its 500 kB chunk warning. With `VITE_SYNQ_API_BASE_URL=http://localhost:8000`, browser review confirmed that the UI renders all four source-only catalog records served by the local recovery backend. No deployed backend connection is claimed. |
| Frontend automated tests | **Pass locally** | `pnpm test` runs 3 test files with 33 passing tests. The suite imports Vitest explicitly, uses deterministic local WebSocket and `fetch` fixtures instead of unavailable services, verifies GraphQL-client fallback behavior, covers an OT redo correction, and verifies configured/unconfigured/malformed source-only catalog responses. These are local unit fixtures, not end-to-end backend integration tests. |
| Backend recovery service | **Health and source-only catalog pass locally** | The backend exposes `GET /`, `GET /health`, `GET /api/v1/examples`, and `GET /api/v1/examples/{example_id}`. `PYTHONPATH=backend python3 backend/tests/health_smoke.py` verifies health plus list, filter, detail, and error contracts through HTTPX ASGI transport. The example route exposes metadata only and explicitly labels all records `source-only-unverified`; it does not compile or execute them. Historical routers, database wiring, GraphQL, WebSocket, ML, and collaboration features remain unavailable. |
| Docker Compose | **Experimental; not verified** | Compose paths and Dockerfile paths point to the existing `backend/` and `frontend/` directories. The review environment returned `DOCKER_UNAVAILABLE`, so neither syntax validation nor an end-to-end container run was possible. In a Docker-capable environment, run `docker compose config --quiet` before attempting `docker compose up --build`. |
| Compiler-core CI workflow | **Passes on GitHub Actions** | `.github/workflows/compiler-core.yml` configures Ubuntu-based build and smoke-test steps for the verified recovery profile. [Compiler Core #29](https://github.com/TangoSplicer/SynQ/actions/runs/31837377648) completed successfully for revision `33e98c2`, restoring the isolated checksum-pinned Mercury toolchain cache, constructing test-only `libsynq_ffi.so`, and reporting 17/17 CTest checks including bounded expression/static-type and Boolean-condition reference coverage, Alpha classical control flow, name resolution, Hybrid IR, Mercury, Clojure/JNA, Common Lisp/CFFI, C, Rust, and OpenQASM validations. This does not validate optional compiler targets or any unimplemented language binding. |
| Frontend CI workflow | **Passes on GitHub Actions** | `.github/workflows/frontend.yml` runs frozen dependency installation, TypeScript checking, Vitest, and production build steps on frontend changes. An initial catalog-integration run exposed that a broad `lib/` ignore rule had omitted the catalog client from Git. After narrowing that rule and adding the source file, the [latest observed frontend validation run](https://github.com/TangoSplicer/SynQ/actions/runs/31706700266) completed successfully for revision `7511e81`. |
| Quantum hardware, hosted services, collaboration, AI automation | **Not verified** | The presence of source files, names, examples, or older documentation does not establish that these capabilities are available or supported. |

## Changes Made During This Review

The recovery work removed a stale pnpm patch reference that prevented frontend installation, added Vite and TypeScript configuration for the actual frontend layout, corrected Docker and Compose paths, and added minimal backend dependency metadata. The repository frontend now installs from a frozen lockfile, type-checks, runs 33 local unit tests, and builds; the former backend-dependent test assumptions were replaced with deterministic client fixtures, and OT redo bookkeeping was corrected. It now lazy-loads non-home routes and interactive examples, emits separate vendor chunks without the previous Vite size warning, presents factual recovery-status messaging, and can render the local source-only catalog only when an explicit backend URL is configured. The backend has a bounded root/health surface, a source-only example catalog with tested list/filter/detail/error behavior, and an executable smoke test. The compiler CMake configuration has explicit optional switches, explicit OpenSSL linkage, and a default core-library profile that avoids known-incomplete subsystems. The recovery parser supports limited declarations plus whitespace-delimited comments, alpha-gated literal-angle parameter syntax, structured diagnostics, typed quantum AST construction, non-evaluating classical declaration expression classification, bounded typed known-gate shape validation, AST source-span provenance, top-level duplicate-declaration rejection, typed measurement parsing, and Alpha-gated Boolean-literal-or-identifier control forms. A strict OpenQASM 3 source exporter lowers typed quantum nodes and typed measurements for its fixed source-export subset, emitting a classical register only when a measurement exists; it makes no execution or SDK claim. The minimal Hybrid IR adds an internal ordered typed representation for the already supported declarations, quantum gates, measurements, and bounded control nodes, with provenance preservation and explicit rejection of retained legacy instructions. A bounded sequential top-level name-resolution pass connects whole-identifier declaration initializers to earlier bindings, propagates only already-known static literal types, preserves opaque expression-like source text, and validates identifier control conditions as earlier Boolean bindings. The seventeen-test compiler suite was remotely validated by [Compiler Core #29](https://github.com/TangoSplicer/SynQ/actions/runs/31837377648).

Generated compiler build files, coverage output, and local IDE metadata have been removed from version control and ignored going forward.

## What Is Not Claimed

This project currently makes no verified claim of a stable release, production deployment, complete API, complete language implementation, working IDE, complete project-wide test suite, coverage percentage, performance benchmark, real quantum-hardware execution, external provider integration, commercial support SLA, or enterprise readiness.

## Recovery Priorities

The next practical phase is **reproducible baseline engineering**:

1. Extend the verified source-span, structured-diagnostics, typed-AST, and minimal Hybrid-IR foundations only through focused semantic increments before broadening the recovery parser grammar.
2. Review C ABI versioning, ownership, and distribution policy before adding any Rust safe wrapper or crate.
3. Extend the verified Hybrid IR and scoped name-resolution foundations with bounded typed classical control flow before broadening parser syntax.
4. Add one bounded backend feature beyond the source-only catalog only after defining its data model, route contract, and tests; do not restore historical routers wholesale.
5. Run an end-to-end container check when a Docker-capable environment is available, and repair or remove stale optional compiler targets one subsystem at a time.

## Historical Documentation

Older phase, architecture, deployment, and feature documents are retained for context. They may contain plans, prototypes, or claims that have not been independently verified. Treat them as historical design material unless they are explicitly updated and linked from this page.
